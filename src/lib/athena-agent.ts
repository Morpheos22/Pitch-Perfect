const ATHENA_MCP_URL = "https://athena-mcp-server.morphylee22.workers.dev/mcp";
const ADAPTIVE_RPC_URL = "https://kal-agent-morpheos255918280.adaptive.ai/api/rpc";
const CLOUDFLARE_MODEL = "@cf/meta/llama-3.3-70b-instruct";
const MAX_HISTORY_MESSAGES = 24;
const MAX_MESSAGE_CHARS = 12000;
export const ATHENA_SYSTEM_PROMPT = `You are Athena, an investor-grade AI diligence and product strategy agent for Pitch Perfect. Be rigorous, concise, and evidence-led. Separate facts from assumptions, never invent facts, and say when information is missing. Use tools when available, but remain helpful without them.`;
type MessageContent = string | Array<{type:"text";text:string}|{type:"image_url";image_url:{url:string}}>;
type Message = {role:"system"|"user"|"assistant"|"tool";content:MessageContent|null;tool_call_id?:string;name?:string;tool_calls?:Array<{id:string;function:{name:string;arguments?:string}}>};
export type AthenaMessage=Message;
function boundedContent(content: MessageContent|null): MessageContent|null { if (typeof content === "string") return content.slice(-MAX_MESSAGE_CHARS); if (Array.isArray(content)) return content.map((part)=>part.type === "text" ? {...part,text:part.text.slice(-MAX_MESSAGE_CHARS)} : part); return content; }
function boundHistory(history: AthenaMessage[]): AthenaMessage[] { return history.slice(-MAX_HISTORY_MESSAGES).map((message)=>({...message,content:boundedContent(message.content)})); }
async function fetchWithTimeout(url:string,init:RequestInit,ms=20000){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),ms);try{return await fetch(url,{...init,signal:controller.signal});}finally{clearTimeout(timer);}}
async function adaptiveChat(messages:Message[]){const key=process.env.ADAPTIVE_API_KEY||process.env.KAL_API_KEY;const response=await fetchWithTimeout(ADAPTIVE_RPC_URL,{method:"POST",headers:{"content-type":"application/json",...(key?{"x-kal-api-key":key}: {})},body:JSON.stringify({jsonrpc:"2.0",id:Date.now(),method:"chat.completions",params:{messages}})});if(!response.ok)throw new Error(`Adaptive RPC returned ${response.status}`);const payload=await response.json() as any;const text=payload.result?.choices?.[0]?.message?.content||payload.result?.response||payload.response;if(typeof text!=="string"||!text.trim())throw new Error("Adaptive RPC returned no response");return text;}
export async function runAthena(messages:Message[],image?:string){const conversation=[{role:"system" as const,content:ATHENA_SYSTEM_PROMPT},...boundHistory(messages)];if(image){const last=conversation[conversation.length-1];if(last?.role==="user")last.content=[{type:"text",text:typeof last.content==="string"?last.content:""},{type:"image_url",image_url:{url:image}}];}try{const account=process.env.CF_ACCOUNT_ID||process.env.CLOUDFLARE_ACCOUNT_ID;const token=process.env.CF_API_TOKEN||process.env.CLOUDFLARE_AI_TOKEN;if(!account||!token)throw new Error("Cloudflare AI credentials are not configured");const response=await fetchWithTimeout(`https://api.cloudflare.com/client/v4/accounts/${account}/ai/v1/chat/completions`,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${token}`},body:JSON.stringify({model:CLOUDFLARE_MODEL,messages:conversation})});if(!response.ok)throw new Error(`Cloudflare AI returned ${response.status}`);const payload=await response.json() as any;const message=payload.choices?.[0]?.message;if(!message)throw new Error("Cloudflare AI returned no message");return {message:message.content||"",usage:payload.usage};}catch(error){console.warn("[Athena] Cloudflare failed; falling back to Adaptive RPC",error);return {message:`[DEGRADED_UNASSISTED_MODE]\n\n${await adaptiveChat(conversation)}`};}}
export async function askAthenaViaPoke(message:string,_context?:unknown,history:AthenaMessage[]=[] ,image?:string){return (await runAthena([...boundHistory(history),{role:"user",content:message}],image)).message;}
export async function askAthenaWithTools(message:string,context?:unknown,history:AthenaMessage[]=[],image?:string){return askAthenaViaPoke(message,context,history,image);}
export async function askAthena(message:string,_context?:unknown,history:AthenaMessage[]=[]){try{return (await runAthena([...history,{role:"user",content:message}])).message;}catch(error){console.warn("[Athena] Provider fallback failed",error);return "I’m sorry, Athena is temporarily unavailable. Please try again in a moment.";}}
